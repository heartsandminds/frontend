{{> components-header }}
{{> components-hero }}
<div class="t-full-width">
    {{> atoms-heading-2:u-underline|u-align-center(heading2Text:"Meet the team") }}
    <div class="t-row-grid">
        <div class="t-3-col-grid">
            {{> components-team-member }}
        </div>
        <div class="t-3-col-grid">
            {{> components-team-member }}
        </div>
        <div class="t-3-col-grid">
            {{> components-team-member }}
        </div>
    </div>
</div>
{{> components-footer }}
